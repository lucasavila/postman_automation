postman_automation:
        image: registry.dev.redbee.io/postman_automation:release
        container_name: "postman_automation"
        ports:
            - "8098:3000"
        volumes:
            - /etc/localtime:/etc/localtime:ro
            - /etc/timezone:/etc/timezone:ro
